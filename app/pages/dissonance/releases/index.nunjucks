{% extends "templates/dissonance/layout.nunjucks" %}

{% macro version_block(version) %}
  <div a="{{ version_number }}" data-version-number="{{ version_number }}" >
    <h5 class="title"><a href="{{ base_path }}/dissonance/releases/{{ version }}.html" title="Dissonance {{ version }}">Dissonance {{ version }}</a></h5>
  </div>
{% endmacro %}

{% block content %}
  
  <ol reversed style="list-style:lower-roman">
  
    {% for item in site.dissonance.children.releases.children.pages | reverse %}
      {% if item.has_version %}
        <li>{% call version_block(item.version.name) -%}{%- endcall %}</li>
      {% endif %}
    {% endfor %}
    
  </ol>
  
{% endblock %}
  